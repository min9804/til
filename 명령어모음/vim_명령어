***vim 명령어***
:split (:sp) 위아래로 창 분할
:vsplit (:vs) 좌우로 창 분할



*문자열 바꾸기
/검색어 입력 
n 다음 결과 N 이전 검색결과

/".*" "로 시작하고 " 로 끝나며 " " 사이에 어떤 문자가 몇 
글자가 오던 전부 매칭

문자열 바꾸기

(시작줄),(끝줄)s/찾을패턴/바꿀문자/옵션
1 ~ 10 줄안에서 test를 find로 바꿈
1,10s/test/find/g
%s/ 모든줄에서 바꿈

바꾸고자 하는 패턴이나 문자열에 / 가 있는 경우 
@, |, ?, # 등으로 바꿔쓸수 있음

g 매칭되는 문자열을 물어보지 않고 변경
i 대소문자를 구분하지 않고 변경
c 매칭되는 문자열마다 바꿀 것인지 사용자에게 물어보고 변경
위의 옵션은 두가지 이상 한번에 지정할 수 있지만 g옵션보다 c옵션이
우선됩니다.

